# =================================================================
# Customer Care Call Processing System - Environment Configuration
# =================================================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!
# =================================================================

# Environment (dev, staging, prod)
ENVIRONMENT=dev

# AWS Configuration
AWS_REGION=us-east-1
AWS_PROFILE=default

# S3 Configuration
# Must be globally unique - add your organization name or ID
S3_BUCKET_NAME=customer-care-calls-dev-YOUR-ORG-UNIQUE-ID

# Google Drive Configuration
# The folder ID from Google Drive URL: https://drive.google.com/drive/folders/<FOLDER_ID>
GDRIVE_FOLDER_ID=your-google-drive-folder-id

# Notifications
# Email to receive CloudWatch alerts
ALERT_EMAIL=your-email@example.com

# Optional: Custom project name (defaults to customer-care-call-processor)
PROJECT_NAME=customer-care-call-processor

# =================================================================
# Lambda Configuration
# =================================================================
WEBHOOK_HANDLER_MEMORY=512
WEBHOOK_HANDLER_TIMEOUT=60
PROCESSING_LAMBDA_MEMORY=1024
PROCESSING_LAMBDA_TIMEOUT=900

# =================================================================
# Monitoring
# =================================================================
LOG_RETENTION_DAYS=30

# =================================================================
# Terraform Variables (auto-mapped)
# =================================================================
TF_VAR_aws_region=${AWS_REGION}
TF_VAR_environment=${ENVIRONMENT}
TF_VAR_project_name=${PROJECT_NAME}
TF_VAR_s3_bucket_name=${S3_BUCKET_NAME}
TF_VAR_gdrive_folder_id=${GDRIVE_FOLDER_ID}
TF_VAR_alert_email=${ALERT_EMAIL}

# =================================================================
# Secrets (stored in AWS Secrets Manager - not in .env)
# =================================================================
# These should be stored securely in AWS Secrets Manager
# Use scripts/setup_google_auth.sh to configure:
#
# - Google Service Account JSON (gdrive-credentials)
# - Webhook Token (gdrive-webhook-config)
#
# DO NOT put actual secrets in this file!

# =================================================================
# Development/Testing Only
# =================================================================

# Run integration tests (requires deployed AWS resources)
# RUN_INTEGRATION_TESTS=false

# Run end-to-end tests (requires full deployment)
# RUN_E2E_TESTS=false

# Log level for local development
# LOG_LEVEL=DEBUG
